import 'package:flutter/material.dart';

class AhadathTap extends StatelessWidget {
  const AhadathTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: Colors.red,);
  }
}
