import 'package:flutter/material.dart';

class AzkarTap extends StatelessWidget {
  const AzkarTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: Colors.black,);
  }
}